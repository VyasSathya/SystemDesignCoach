import { ArrowLeft, ChevronUp, Send, ClipboardList } from 'lucide-react';

// ... in your JSX where icons are used:
<button>
  <ArrowLeft size={24} /> {/* Make sure to specify size */}
</button>

<button>
  <ChevronUp size={24} />
</button>

<button>
  <Send size={24} />
</button>

<button>
  <ClipboardList size={24} />
</button>